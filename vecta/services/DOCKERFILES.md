# =============================================================================
# Vecta Platform — Dockerfile collection
# Each service has its own Dockerfile for multi-stage production builds.
# Copy each block to its respective service directory.
# =============================================================================

# ---
# USAGE: Each file is shown with its target path as a comment header.
# The docker-compose.yml references these via the `build.context` field.
# ---
